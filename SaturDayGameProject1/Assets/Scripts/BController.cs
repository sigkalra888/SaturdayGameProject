using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BController : MonoBehaviour
{
    public void BlockMove(ScriptableObject sObj)
    {
        if (Input.GetKeyDown(KeyCode.LeftArrow))
        {
            //sObj.
        }
    }
}
